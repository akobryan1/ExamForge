/**
 * AI provider configuration.
 *
 * The DeepSeek API key used for essay grading is temporarily replaced with the
 * placeholder below so no live credential is used. To restore real AI calls,
 * swap `PLACEHOLDER_AI_API_KEY` back for `process.env.DEEPSEEK_API_KEY` at the
 * call sites in `routes/exams.ts` and `routes/settings.ts`.
 */
export const PLACEHOLDER_AI_API_KEY = 'sk-placeholder-key-replace-in-production';
