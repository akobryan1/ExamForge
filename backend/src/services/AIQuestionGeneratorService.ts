/*
 * [AI QUESTION GENERATOR SERVICE REMOVED — see git history for Generate with AI feature]
 * 
 * This service generated exam questions from study material using OpenAI/OpenRouter.
 * It was used by the /api/ai/generate-questions and /api/ai/generate-from-file routes.
 * The mock question generator, prompt builder, and PDF extraction helpers are all preserved in git history.
 * 
 * Revisit when the Generate with AI feature is re-enabled.
 */

import { QuestionType } from '../types/exam';

export class AIQuestionGeneratorService {
}
