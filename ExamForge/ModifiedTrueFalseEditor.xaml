<UserControl x:Class="ExamForge.ModifiedTrueFalseEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <StackPanel>
        <TextBlock Text="Answer Configuration"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimary}"
                   Margin="0,0,0,12"/>

        <!-- True/False Selection -->
        <TextBlock Text="Statement is:"
                   FontSize="12"
                   Foreground="{StaticResource TextSecondary}"
                   Margin="0,0,0,8"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Border Background="{StaticResource SurfaceLight}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12,8"
                    Margin="0,0,12,0"
                    Cursor="Hand"
                    MouseLeftButtonDown="TrueOption_Click">
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="TrueRadio"
                                 GroupName="TrueFalse"
                                 VerticalAlignment="Center"
                                 Margin="0,0,8,0"/>
                    <TextBlock Text="TRUE"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource SurfaceLight}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12,8"
                    Cursor="Hand"
                    MouseLeftButtonDown="FalseOption_Click">
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="FalseRadio"
                                 GroupName="TrueFalse"
                                 VerticalAlignment="Center"
                                 Margin="0,0,8,0"/>
                    <TextBlock Text="FALSE"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Correction/Modification Text -->
        <TextBlock Text="If false, what should it be? (Correction)"
                   FontSize="12"
                   Foreground="{StaticResource TextSecondary}"
                   Margin="0,0,0,8"/>
        <Border Background="{StaticResource SurfaceLight}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <TextBox x:Name="ModificationTextBox"
                     Background="Transparent"
                     Foreground="{StaticResource TextPrimary}"
                     BorderThickness="0"
                     MinHeight="80"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     TextChanged="Modification_TextChanged"/>
        </Border>

        <!-- Custom Points -->
        <Border Background="{StaticResource SurfaceLight}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,16,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Custom Points (optional)"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondary}"
                           VerticalAlignment="Center"/>
                <TextBox x:Name="CustomPointsTextBox"
                         Grid.Column="1"
                         Background="{StaticResource SurfaceDark}"
                         Foreground="{StaticResource TextPrimary}"
                         BorderBrush="{StaticResource BorderColor}"
                         BorderThickness="1"
                         Padding="8,6"
                         TextAlignment="Center"
                         TextChanged="CustomPoints_TextChanged">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>