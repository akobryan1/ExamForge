<UserControl x:Class="ExamForge.essay_checker_usercontrol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Margin="0"
             Loaded="EssayChecker_Loaded">
    <Grid Background="{StaticResource BackgroundDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceDark}" Padding="16" CornerRadius="8" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Essay Checker" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock x:Name="QueueCountText" Text="(0 pending)" Margin="10,0,0,0" Foreground="{StaticResource TextSecondary}"/>
                </StackPanel>

                <ComboBox x:Name="EssayExamFilter" Grid.Column="1" Width="220" Margin="12,0,0,0" SelectionChanged="EssayExamFilter_SelectionChanged"/>
                <Button Grid.Column="2" Content="Auto-Grade All Pending Essays" Margin="12,0,0,0" Padding="12,8" Style="{StaticResource PrimaryButtonStyle}" Click="AutoGradeAll_Click"/>
                <Button Grid.Column="3" Content="Save Current Grade" Margin="12,0,0,0" Padding="12,8" Style="{StaticResource SecondaryButtonStyle}" Click="SaveCurrentGrade_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Review Queue" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                    <ListBox x:Name="EssayQueueList" DisplayMemberPath="DisplayLabel" Height="250" SelectionChanged="EssayQueueList_SelectionChanged"/>

                    <TextBlock Text="Graded Archive" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,14,0,10"/>
                    <ListBox x:Name="GradedArchiveList" DisplayMemberPath="DisplayLabel" Height="250" SelectionChanged="GradedArchiveList_SelectionChanged"/>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="14" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="Prompt &amp; Rubric" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                            <TextBlock x:Name="ExamMetaText" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <TextBlock Text="Essay Question / Statement" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="QuestionTextBlock" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,4,0,10"/>
                            <TextBlock Text="Rubric" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="RubricTextBlock" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,4,0,10"/>
                            <TextBlock Text="Model Answer / Key Points" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock x:Name="ModelAnswerTextBlock" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="14" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Student Essay" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                            <TextBlock x:Name="StudentEssayText" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="1" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="14" Margin="0,12,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="AI Score:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="AiScoreText" Margin="6,0,0,0" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource AccentGold}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="ConfidenceText" Margin="14,0,0,0" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="FlagReviewText" Margin="14,0,0,0" Foreground="{StaticResource WarningYellow}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="-1" Width="42" Margin="8,0,0,0" Click="DecreaseScore_Click"/>
                            <Button Grid.Column="2" Content="+1" Width="42" Margin="8,0,0,0" Click="IncreaseScore_Click"/>
                            <TextBox x:Name="AdjustmentReasonTextBox" Grid.Column="3" Width="260" Margin="8,0,0,0" ToolTip="Instructor adjustment reason"/>
                        </Grid>

                        <DataGrid x:Name="RubricBreakdownGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  Height="170"
                                  Margin="0,0,0,10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Criterion" Binding="{Binding Criterion}" Width="*"/>
                                <DataGridTextColumn Header="Weight" Binding="{Binding Weight, StringFormat='{}{0:F0}%'}" Width="80"/>
                                <DataGridTextColumn Header="Max" Binding="{Binding MaxPoints, StringFormat='{}{0:F1}'}" Width="70"/>
                                <DataGridTextColumn Header="Awarded" Binding="{Binding PointsAwarded, StringFormat='{}{0:F1}'}" Width="80"/>
                                <DataGridTextColumn Header="Justification" Binding="{Binding Reason}" Width="2*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock Text="Auto-grade Justification" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock x:Name="JustificationText" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceDark}" CornerRadius="8" Padding="10" Margin="0,12,0,0">
            <TextBlock Text="DeepSeek grading uses configured rubric + model answer + key points. API key placeholder is configured in EssayAutoGradingService." Foreground="{StaticResource TextMuted}" FontSize="11"/>
        </Border>
    </Grid>
</UserControl>
