<Window x:Class="ExamForge.Views.ViewSubmissionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student Test Submission"
        Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}"
        ResizeMode="CanResize">
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="0,0,0,1" 
                Padding="25,20">
            <StackPanel>
                <TextBlock x:Name="StudentNameText" FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock x:Name="ExamInfoText" FontSize="13" 
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="{StaticResource HeaderBackgroundBrush}" 
                            CornerRadius="6" Padding="12,6" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Score: " FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="ScoreText" FontSize="12" FontWeight="SemiBold" 
                                       Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="1" Background="{StaticResource HeaderBackgroundBrush}" 
                            CornerRadius="6" Padding="12,6" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Time Taken: " FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="TimeText" FontSize="12" FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="2" Background="{StaticResource HeaderBackgroundBrush}" 
                            CornerRadius="6" Padding="12,6" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Status: " FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="StatusText" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Content Area - Questions and Answers -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
            <ItemsControl x:Name="QuestionsContainer">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource CardBackgroundBrush}" 
                                BorderBrush="{StaticResource BorderBrushLight}" 
                                BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                            <StackPanel>
                                <!-- Question Header -->
                                <DockPanel Margin="0,0,0,12">
                                    <Border DockPanel.Dock="Left" Background="{StaticResource PrimarySoftBrush}" 
                                            CornerRadius="4" Padding="8,4" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding QuestionNumberText}" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="{StaticResource PrimaryBrush}"/>
                                    </Border>
                                    <Border DockPanel.Dock="Right" Background="{Binding CorrectnessBackground}" 
                                            CornerRadius="4" Padding="8,4">
                                        <TextBlock Text="{Binding CorrectnessText}" FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{Binding CorrectnessForeground}"/>
                                    </Border>
                                    <TextBlock Text="{Binding QuestionType}" FontSize="11" 
                                               Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                </DockPanel>

                                <!-- Question Text -->
                                <TextBlock Text="{Binding QuestionText}" FontSize="14" 
                                           Foreground="{StaticResource TextPrimaryBrush}" 
                                           TextWrapping="Wrap" Margin="0,0,0,12"/>

                                <!-- Student Answer -->
                                <Border Background="{StaticResource HeaderBackgroundBrush}" 
                                        CornerRadius="6" Padding="15" Margin="0,0,0,10">
                                    <StackPanel>
                                        <TextBlock Text="Student's Answer:" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding StudentAnswer}" FontSize="13" 
                                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <!-- Correct Answer (if wrong) -->
                                <Border Background="{StaticResource SuccessBackgroundBrush}" 
                                        CornerRadius="6" Padding="15"
                                        Visibility="{Binding ShowCorrectAnswer, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="Correct Answer:" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="{StaticResource SuccessTextBrush}" Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding CorrectAnswer}" FontSize="13" 
                                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <!-- Points -->
                                <TextBlock Text="{Binding PointsText}" FontSize="12" 
                                           Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="0,1,0,0" 
                Padding="25,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Export Student Report" Width="170" Height="35" Margin="0,0,10,0"
                        Style="{StaticResource PrimaryButtonStyle}" Click="ExportReport_Click"/>
                <Button Content="Export PDF" Width="120" Height="35" Margin="0,0,10,0"
                        Style="{StaticResource SecondaryButtonStyle}" Click="ExportPdf_Click"/>
                <Button Content="Close" Width="100" Height="35" 
                        Style="{StaticResource SecondaryButtonStyle}" IsCancel="True" Click="Close_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
