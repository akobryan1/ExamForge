<Window x:Class="ExamForge.Views.BroadcastMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Broadcast Message to Students"
        Height="400" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}"
        ResizeMode="NoResize">
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="0,0,0,1" 
                Padding="25,20">
            <StackPanel>
                <TextBlock Text="Broadcast Message" FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock x:Name="SessionInfoText" FontSize="13" 
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="25,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Message to Students:" FontSize="14" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
            
            <TextBox x:Name="MessageTextBox" Grid.Row="1" 
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="12" FontSize="13"
                     Background="{StaticResource CardBackgroundBrush}"
                     BorderBrush="{StaticResource BorderBrushLight}"
                     Foreground="{StaticResource TextPrimaryBrush}"
                     MaxLength="500"/>
            
            <TextBlock Grid.Row="2" Text="Characters remaining: " FontSize="11" 
                       Foreground="{StaticResource TextMutedBrush}" Margin="0,5,0,0">
                <Run x:Name="CharCountText" Text="500"/>
                <Run Text="/500"/>
            </TextBlock>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushLight}" BorderThickness="0,1,0,0" 
                Padding="25,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="100" Height="35" Margin="0,0,10,0"
                        Style="{StaticResource SecondaryButtonStyle}" IsCancel="True" Click="Cancel_Click"/>
                <Button Content="Send Message" Width="130" Height="35" 
                        Style="{StaticResource PrimaryButtonStyle}" Click="SendMessage_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
